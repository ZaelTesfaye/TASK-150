# questions.md

## 1. Initial Role Bootstrap

**Question:** How is the very first Administrator account created on a fresh install with no network or external identity provider?
**Assumption:** The first app launch requires an offline bootstrap flow that creates a single seed Administrator, and additional users are created by an Administrator only.
**Solution:** Implemented a first-run bootstrap wizard gated by a one-time local flag; after completion, user creation is permission-gated under Administrator actions.

---

## 2. Authentication Policy in Offline Mode

**Question:** What authentication mechanism is required for local accounts, including password rules and lockout behavior?
**Assumption:** Local username/password auth is mandatory, with minimum complexity, 5 failed attempts lockout for 15 minutes, and lockout persisted across app restarts.
**Solution:** Implemented local credential auth with salted password hashing, persistent failed-attempt counters, timed lockout, and audit logging for login success/failure.

---

## 3. Actor Identity for Audit Events

**Question:** What exact actor identity must be captured when writing timestamped actor IDs for state-changing operations?
**Assumption:** Every write stores `actorUserId`, `actorRole`, and `deviceId`; system-triggered actions use a reserved `SYSTEM` actor.
**Solution:** Added mandatory actor metadata columns to audited writes and blocked state transitions when actor metadata is missing.

---

## 4. End User Age Range Encoding

**Question:** What valid age-range buckets are supported for profile creation and targeting logic?
**Assumption:** Age is stored as enumerated ranges (`13-17`, `18-25`, `26-35`, `36-50`, `51+`) rather than free text.
**Solution:** Implemented enum-backed age-range fields with validation and migration-safe constants.

---

## 5. Dietary Pattern Vocabulary

**Question:** Which dietary pattern values are allowed to avoid inconsistent free-form entries?
**Assumption:** Allowed set is fixed (`omnivore`, `vegetarian`, `vegan`, `pescatarian`, `keto`, `halal`, `kosher`, `other`).
**Solution:** Implemented controlled vocab selection with optional note for `other`; all matching/filtering uses normalized enum values.

---

## 6. Calorie and Macro Calculation Method

**Question:** Which formula and constraints are used to compute daily calorie budget and macro targets from a weekly goal?
**Assumption:** Calorie target is based on estimated TDEE with goal delta, and macros are generated by configurable ratio presets with hard min/max bounds.
**Solution:** Implemented deterministic calculation with explicit constants, rounding rules, and persisted calculation provenance in plan metadata.

---

## 7. Weekly Plan Date Boundaries

**Question:** How are weekly plans anchored (calendar week vs rolling 7-day window), and what timezone is authoritative?
**Assumption:** Plans are rolling 7-day windows starting from user-selected local date in device timezone.
**Solution:** Implemented date handling using local timezone day boundaries with explicit start/end instants saved alongside display dates.

---

## 8. Meal Swap Equivalence Selection

**Question:** When multiple meals satisfy ±10% calories and ±5 g protein, what ranking and tie-break rules apply?
**Assumption:** Candidates are ranked by smallest calorie delta, then protein delta, then lowest sodium for low-sodium users.
**Solution:** Implemented deterministic ranking and stable tie-break ordering so repeated queries return identical swap results.

---

## 9. Explainability Reason Catalog

**Question:** Which standardized reason codes are required for each recommendation to guarantee at least two explainable reasons?
**Assumption:** Reasons are generated from a controlled catalog (budget fit, macro alignment, allergy-safe, low-sodium match, preferred time fit, dietary pattern match).
**Solution:** Implemented reason-code generation and validation that rejects recommendations with fewer than two non-duplicate reasons.

---

## 10. Learning Plan Status Transition Matrix

**Question:** Which status transitions are valid between not started, in progress, paused, completed, and archived?
**Assumption:** Allowed transitions are explicitly constrained (`not started -> in progress/archived`, `in progress -> paused/completed/archived`, `paused -> in progress/archived`, `completed -> archived`), with no direct edit after completed.
**Solution:** Implemented a transition matrix validator with confirmation prompts and hard-blocked invalid transitions.

---

## 11. Completed Plan Edit Exception via Duplication

**Question:** What exact duplication behavior is required when a completed plan must be modified?
**Assumption:** Duplication creates a new draft plan with copied meals/rules, new IDs, status `not started`, and backlink to source plan.
**Solution:** Implemented duplicate-and-edit workflow and prevented in-place edits on completed records at repository and UI levels.

---

## 12. Frequency Validation Scope

**Question:** Should the “exceeding 7 days/week” validation apply only to study frequency, meal frequency, or both recurring schedules and reminders?
**Assumption:** Validation applies to all weekly recurrence definitions tied to plans and reminders.
**Solution:** Implemented shared validator used by plan, schedule, and reminder forms to enforce max 7 occurrences per week.

---

## 13. Canary Rollout User Selection

**Question:** How should the 10% local canary cohort be selected so assignment is stable and reproducible offline?
**Assumption:** Cohort membership uses deterministic hashing of `(rolloutId + userId + deviceSalt)` with threshold at 10%.
**Solution:** Implemented deterministic bucketing and persisted assignment snapshots for audit and back-calculation consistency.

---

## 14. Config Version Activation and Rollback

**Question:** What activation lifecycle is required for configuration versions (draft, active, retired) and rollback behavior?
**Assumption:** Only one active version per config domain; rollback means activating a prior immutable version without mutation.
**Solution:** Implemented version states, activation guards, and rollback-by-repointing to historical versions.

---

## 15. Rule Evaluation Ordering and Conflict Resolution

**Question:** If multiple threshold rules trigger conflicting actions, what precedence model should be used?
**Assumption:** Rules execute by priority, then creation time; highest priority decisive action wins, with conflict recorded.
**Solution:** Implemented priority-based evaluator with deterministic ordering and conflict annotations in evaluation logs.

---

## 16. Hysteresis Semantics

**Question:** Is hysteresis stateful per entity, and how should enter/exit thresholds be tracked over time?
**Assumption:** Hysteresis is stateful per metric key and target entity, requiring prior state persistence to avoid flapping.
**Solution:** Implemented per-entity hysteresis state tables and transition logic using enter at 80% and exit at 90%.

---

## 17. Minimum Duration Rule Clock

**Question:** Does the 10-minute minimum duration require continuous condition truth or cumulative truth within a window?
**Assumption:** Condition must remain continuously true for 10 uninterrupted minutes.
**Solution:** Implemented continuous-duration timers that reset on any violating sample.

---

## 18. Effective Time Window Timezone and DST

**Question:** Which timezone governs rule effective windows and business-hour calculations, especially across daylight-saving transitions?
**Assumption:** Device-local timezone is authoritative; windows are evaluated in local civil time with DST-aware conversions.
**Solution:** Implemented timezone-aware window checks using stored zone IDs and DST-safe interval calculations.

---

## 19. Back-Calculation Data Source and Reproducibility

**Question:** Should historical back-calculation use current rule versions or the rule versions effective at historical event time?
**Assumption:** Back-calculation replays historical facts with rule versions effective at each historical timestamp.
**Solution:** Implemented time-travel rule resolution by effective window and version to produce reproducible historical outcomes.

---

## 20. Ticket Lifecycle and SLA Clock Rules

**Question:** What ticket states and SLA pause/resume rules are required for delay/dispute/lost-item flows?
**Assumption:** States include `new`, `triaged`, `awaiting_user`, `in_progress`, `resolved`, `closed`; SLA pauses in `awaiting_user` and resumes otherwise.
**Solution:** Implemented explicit state machine, SLA timers, and breach flags for first response (4 business hours) and resolution target (3 business days).

---

## 21. Business Calendar Definition

**Question:** What defines business hours and non-working days for SLA calculations in fully offline mode?
**Assumption:** Business hours are 09:00-18:00 local Monday-Friday; weekends are excluded and local holiday tables are optional configuration.
**Solution:** Implemented offline business-calendar engine with configurable workdays/hours and optional holiday overrides.

---

## 22. Compensation Suggestion Guardrails

**Question:** How is automated compensation amount selected within $3-$20 and what policy gates Agent approval above $10?
**Assumption:** Suggestion is rule-based by exception severity and SLA breach; >$10 requires explicit Agent approval before commit.
**Solution:** Implemented policy engine that computes suggested amount, enforces threshold approval workflow, and logs approvals immutably.

---

## 23. Evidence Upload Constraints

**Question:** What are the exact file/type/size constraints for evidence uploads when only image/text is allowed?
**Assumption:** Images are JPEG/PNG/WebP up to 10 MB each; text evidence is UTF-8 plain text up to 20 KB.
**Solution:** Implemented MIME/type/size validation, downsampling for large images, and scoped-storage URI persistence with hash fingerprints.

---

## 24. Immutable Audit Trail Enforcement

**Question:** What technical controls define immutability for audit records in a local SQLite environment?
**Assumption:** Audit rows are append-only, updates/deletes are prohibited at DAO level and blocked by DB triggers.
**Solution:** Implemented append-only schema with `BEFORE UPDATE/DELETE` trigger aborts and integrity checks in repository tests.

---

## 25. PII Masking and Agent Reveal Logging

**Question:** Which ticket fields are considered PII and what exactly must be logged when an Agent reveals masked data?
**Assumption:** Name, phone, address, and free-text identifiers are masked by default; reveal event logs actor, field scope, ticket ID, timestamp, and reason.
**Solution:** Implemented field-level masking plus Agent-only reveal action requiring reason input and mandatory audit entry.

---

## 26. Reminder Cap and Quiet-Hour Deferral Rules

**Question:** How should reminders behave when events occur during quiet hours and daily cap (3/day) is reached?
**Assumption:** Messages generated in quiet hours are deferred to 07:00 local; if cap reached, lower-priority reminders are dropped, not queued indefinitely.
**Solution:** Implemented per-user daily counters, priority ordering, quiet-hour deferral, and deterministic drop policy with audit traces.

---

## 27. WorkManager Scheduling Constraints

**Question:** What exact WorkManager constraints satisfy “schedule only when device is idle” while remaining reliable offline?
**Assumption:** Deferred reminder and heavy backfill work require `requiresDeviceIdle=true`, battery-not-low, and persisted unique work policies.
**Solution:** Implemented constrained workers, idempotent work keys, and fallback next-idle rescheduling for missed windows.

---

## 28. Transaction Boundaries for Critical Writes

**Question:** Which multi-entity write operations must be atomic to satisfy reconciliation and state-transition integrity?
**Assumption:** Any billing/order/ticket mutation and related audit/reconciliation updates must commit in one SQL transaction.
**Solution:** Implemented repository-level transactional use cases that write domain state + reconciliation state + audit event atomically.

---

## 29. Reconciliation State Machine Definition

**Question:** What reconciliation states and transitions are required for orders, billing events, and ticket-linked adjustments?
**Assumption:** States are `pending`, `matched`, `mismatched`, `adjusted`, `finalized`, with timestamped transitions and actor IDs.
**Solution:** Implemented explicit reconciliation transition rules and invariant checks that reject invalid jumps.

---

## 30. Performance Acceptance Method for 1,000,000 Rows

**Question:** What benchmark method proves typical indexed lookups stay under 50 ms on mid-range devices?
**Assumption:** Performance is validated with seeded local datasets up to 1,000,000 rows and repeatable instrumentation tests for hot queries.
**Solution:** Implemented seeded benchmark suite, composite indexes (`userId,date`, `status,updatedAt`), query plans, and regression thresholds in CI tests.

---

## 31. Legacy List Usage Boundary

**Question:** Which screens must keep RecyclerView + DiffUtil versus Compose lazy lists to satisfy mixed UI requirements?
**Assumption:** Existing legacy fragments keep RecyclerView+DiffUtil; all new screens use Compose lazy components.
**Solution:** Implemented a screen-level list registry documenting list technology choice and enforced it in UI module reviews/tests.

---

## 32. Image Cache Policy and OOM Protection

**Question:** How should the 20 MB LRU cache interact with evidence image downsampling and lifecycle memory pressure callbacks?
**Assumption:** All image loads are downsampled to display size before caching; cache trims aggressively on `TRIM_MEMORY_RUNNING_LOW` and above.
**Solution:** Implemented bounded in-memory LRU cache (20 MB), decode sampling by target bounds, and lifecycle-driven cache trimming hooks.
